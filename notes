<div class="row dum">
	<pre><?php var_dump(the_post_thumbnail_url()); ?></pre>
</div> <!-- dum -->

$video_posts = array(
			'post_type' => 'video_posts',
			'terms'		=> $all_terms[$x]->term_id;
			);